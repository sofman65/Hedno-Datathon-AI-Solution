# Minimal dependencies for the HEDNO power-theft detection pipeline.
# The original datathon run used Azure ML (Python 3.8, hdbscan 0.8.29, catboost 1.1.1);
# the ranges below are chosen to also work on modern Python (3.9-3.11).

# Core
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2

# Geospatial clustering
hdbscan>=0.8.29

# Gradient-boosted detectors
xgboost>=1.7
lightgbm>=3.3
catboost>=1.1.1

# Class imbalance utilities (imported in notebooks)
imbalanced-learn>=0.10

# DQN experiment (optional — only needed for DQN.ipynb / the agent stage of Main.ipynb)
tensorflow>=2.12
dill>=0.3.6

# Notebooks
jupyter
